{
  "name": "druse-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Pruebas de punta a punta: la aplicación entera, por donde la usa una persona.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2"
  }
}
